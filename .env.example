DASHBOARD_USER=shubh
DASHBOARD_PASSWORD=changeme

GCP_PROJECT_ID=project-27a0842e-5c6f-4cc2-805
GCP_ZONE=us-central1-a
GCP_INSTANCE_NAME=firecrawl-host

GCP_SSH_USER=vps-control
GCP_SSH_KEY_PATH=/root/.ssh/gcp-firecrawl-poll

PORT=4000

# Bearer key required by firecrawl.shubhbuilds.com -- put this same value in
# every tool's FIRECRAWL_API_KEY. Generate with:
#   node -e "console.log('fcrelay-' + require('crypto').randomBytes(24).toString('hex'))"
FIRECRAWL_RELAY_KEY=
