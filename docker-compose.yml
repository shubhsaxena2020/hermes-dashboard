services:
  vps-control:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    expose:
      - "4000"
    networks:
      - proxy
networks:
  proxy:
    external: true
